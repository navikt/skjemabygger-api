server:
  port: 8082

spring:
  application:
    name: forms-api
  main:
    banner-mode: off
  jpa:
    open-in-view: false
  flyway:
    enabled: true
  profiles:
    default: "local"
    group:
      prod:
      - "gcpdb"
      - "azure"
      preprod:
      - "gcpdb"
      - "azure"
