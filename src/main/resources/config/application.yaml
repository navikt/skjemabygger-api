server:
  port: 8082

spring:
  application:
    name: forms-api
  main:
    banner-mode: off
  jpa:
    open-in-view: false
  flyway:
    enabled: true

forms-api:
  azure:
    groups:
      user-group-id: ${FORMS_API_AD_GROUP_USER:mock-user-group-id}
      admin-group-id: ${FORMS_API_AD_GROUP_ADMIN:mock-admin-group-id}
